from django.apps import AppConfig

# register GO term enrichment plottin app
class EnrichmentplotterConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "enrichmentplotter"
